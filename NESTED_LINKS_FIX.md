# 🔍 Откуда берутся баги со ссылками на вложенных страницах

## Проблема

Если со главной страницы ссылки работают, а по клику переходим на другие страницы — там ничего не работает — это исключительно типичная проблема с **относительными путями** при глубокой вложенности.

### Цитата: По примеру от этого пользователя

❌ **Главная страница** `/index.html` → ссылки работают ✅  
❌ **Вложенная** `/services/design/index.html` → ссылки биты ❌

---

## Почему ЭТО ПОРОЖДАЕТСЯ?

### Как браузер решает относительные пути

Релативные пути решаются относительно текущего **директория** страницы:

#### Сценарий 1: Главная страница

```html
<!-- Файл: /index.html -->
<!-- Текущий директорий: / -->

<a href="./about.html">About</a>
<!-- Решается как: / + about.html = /about.html ✅ -->

<a href="./services/design.html">Design</a>
<!-- Решается как: / + services/design.html = /services/design.html ✅ -->
```

#### Сценарий 2: Вложенная страница

```html
<!-- Файл: /services/design/index.html -->
<!-- Текущий директорий: /services/design/ -->

<a href="./about.html">About</a>
<!-- Решается как: /services/design/ + about.html = /services/design/about.html ❌ -->
<!-- WRONG! этот файл не существует -->

<a href="./services/design.html">Design</a>
<!-- Решается как: /services/design/ + services/design.html = /services/design/services/design.html ❌ -->
```

🚨 **ПОГОВОРКА**: Когда ты переходишь на вложенную страницу, текущие директории для резолюции **изменяются**! Это базовое поведение всех браузеров.

---

## ✅ РЕШЕНИЕ: `<base href>` тег

Тег `<base>` рассказывает браузеру: **все относительные пути решая нЕ от текущего директория, а от зАданного**.

### Как это работает

```html
<!-- В ЛЮБОМ файле (index.html, /services/design/index.html, и т.д.) -->

<head>
    <meta charset="UTF-8">
    <base href="/">  ← МАГичная строка!
    <title>Page</title>
</head>

<body>
    <!-- А теперь ВСЕ эти ссылки решаются от рута (/) -->
    <a href="./about.html">About</a>
    <!-- Теперь: / + about.html = /about.html ✅ (ВЕЗДЕ!) -->
    
    <a href="./services/design.html">Design</a>
    <!-- Теперь: / + services/design.html = /services/design.html ✅ (ВЕЗДЕ!) -->
</body>
```

### Почему это важно?

| Сценарий | Без `<base>` | С `<base href="/">` |
|---------|---------------|-------------------|
| `/index.html` → `./about.html` | ✅ /about.html | ✅ /about.html |
| `/services/design/index.html` → `./about.html` | ❌ /services/design/about.html | ✅ /about.html |
| `/services/design/index.html` → `./services/design.html` | ❌ /services/design/services/design.html | ✅ /services/design.html |

🚀 **РЕзУЛЬТАТ**: Одна большая строка решает все проблемы со ссылками на вложенных страницах!

---

## 🔧 Ото реализовано в fix-static-site.py (v3.3.0)

### Метод: `inject_base_tag()`

```python
def inject_base_tag(self, soup: BeautifulSoup, base_href: str = "/") -> bool:
    """⭐ NEW: Inject <base href> tag to fix nested link issues."""
    head = soup.find('head')
    if not head:
        return False
    
    # Проверяем, это бы что тег ещё не был добавлен
    existing_base = head.find('base')
    if existing_base:
        return False
    
    # Создаём тег и вставляем после charset (best practice)
    base_tag = soup.new_tag('base', href=base_href)
    charset = head.find('meta', charset=True)
    if charset:
        charset.insert_after(base_tag)
    else:
        head.insert(0, base_tag)
    
    self.base_tags_added += 1
    return True
```

### Как используется

```python
def process_html_file(self, file_path, cwd, base_href="/"):
    # ...
    soup = BeautifulSoup(content, "lxml")
    
    # ⭐ NEW: Это выполняется ДЛЯ КАЖДОГО файла
    if self.inject_base_tag(soup, base_href):
        modified = True
```

### Вывод в отчёт

```
⭐ Added <base href="/"> tags: 45 files
   → Fixes nested page link issues
```

---

## ⚠️ Осталось ещё

### Такое сюда бывает?

Как бывает, на некоторых сайтах выпадают сылки без расширения, но с относительными путями это будет работать.

### Но что если юрл требуют .html?

Обычно это не проблема для GitHub Pages тк она их ребрандирует автоматически.

### Но что если мне нужно `/project/` (субпать)?

Никаких проблем! До постави в деплой:

```bash
gh workflow run deploy.yml -f run_id=12345 -f base_href="/project/"
```

И тег автоматически будет:

```html
<base href="/project/">
```

Перевод всех относительных путей в `/project/` из рута.

---

## 🔍 Диагностика

Если ссылки ещё не работают и `<base>` уже анный — чек
1. **Открой DevTools** (F12)
2. **Найди `<base>` тег**:
   ```html
   <base href="/">  ✅ НОРМАЛьно
   ```
3. **Проверь URL**:
   - Откры ссылку
   - Нажми F12 → Console
   - Повисней над ссылкой (текущий URL должн быть правильный)

---

## ✅ Нормальные сценарии

### 1️⃣ Root deployment (/):
```python
fixer.run(base_href="/")
# Ввод: <base href="/">
```

### 2️⃣ Subpath deployment (/project/):
```python
fixer.run(base_href="/project/")
# Ввод: <base href="/project/">
```

### 3️⃣ Custom domain (https://example.com/):
```python
# Нормально не требуется - относительные целевые ресурсы
# работают на любом domain
fixer.run(base_href="/")
# <base href="/"> проретаяет к https://example.com/
```

---

## 🌟 Почему это было опускано?

У ревыра аэл-юаеп? Бывает и такое. Это **стандартные инструменты HTML**, базовые вещи по стандартам **W3C**.

Экспорт истории WordPress часто не въёжэны с необходимо выюставленым поддержкой.

---

**Версия**: v3.3.0 | **Дата**: 2026-01-02
