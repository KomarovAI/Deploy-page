# 🚀 Руководство по деплою

## GitHub Pages

### Автоматический деплой

1. **Настройка репозитория**
   ```bash
   # Убедитесь, что репозиторий публичный
   # Settings > General > Danger Zone > Change visibility
   ```

2. **Включение GitHub Pages**
   - Settings > Pages
   - Source: GitHub Actions
   - Branch: main

3. **Push изменений**
   ```bash
   git add .
   git commit -m "feat: initial deployment"
   git push origin main
   ```

4. **Ваш сайт доступен по адресу:**
   ```
   https://komarovai.github.io/Deploy-page/
   ```

### Кастомный домен (опционально)

1. Добавьте CNAME файл:
   ```bash
   echo "yourdomain.com" > CNAME
   git add CNAME
   git commit -m "docs: add custom domain"
   git push
   ```

2. Настройте DNS:
   ```
   A Record: 185.199.108.153
   A Record: 185.199.109.153
   A Record: 185.199.110.153
   A Record: 185.199.111.153
   ```

---

## Альтернативные платформы

### Netlify

```bash
# Установка CLI
npm install -g netlify-cli

# Деплой
netlify deploy --prod --dir=src
```

### Vercel

```bash
# Установка CLI
npm install -g vercel

# Деплой
vercel --prod
```

### Cloudflare Pages

1. Подключите репозиторий
2. Build command: (оставить пустым)
3. Output directory: `src`

---

## CI/CD Workflow

Файл `.github/workflows/deploy.yml` автоматически:

1. Запускается при push в `main`
2. Собирает статические файлы
3. Публикует на GitHub Pages

Никаких дополнительных действий не требуется!

---

## Troubleshooting

### Сайт не обновляется

```bash
# Очистить кэш GitHub
Actions > Re-run all jobs
```

### 404 Error

- Проверьте, что репозиторий публичный
- Убедитесь, что Pages включены в настройках
- Подождите 2-3 минуты после первого деплоя

### HTTPS не работает

- GitHub автоматически выпускает сертификат
- Ожидание: до 24 часов для кастомного домена

---

<div align="center">

[← Вернуться к README](../README.md)

</div>